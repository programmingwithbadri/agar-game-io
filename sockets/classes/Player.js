class Player {
  constructor(playerId, playerConfig, playerData) {
    this.playerId = playerId;
    this.playerConfig = playerConfig;
    this.playerData = playerData;
  }
}

module.exports = Player;